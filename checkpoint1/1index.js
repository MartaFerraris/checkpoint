const words = ['spray', 'limit', 'elite', 'exuberant', 'present'];

// function uppercaseWord(arr) {
    
// }

words.forEach((word)=> {
    word.toUpperCase();
    
    console.log(word.toUpperCase());
})